# Mobile Projects (Expo / React Native — build con EAS)

> El móvil NO se despliega en Vercel. Se versiona aquí y se compila con `eas build`.

## Reglas:
1. Una carpeta por app: `01-fuerza-ventas-app`
2. Cada app es un proyecto Expo independiente (`npx create-expo-app`).

## Próximo:
- [ ] `01-fuerza-ventas-app` — catálogo offline + pedidos + sync
