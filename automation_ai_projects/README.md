# Automation + AI Projects (n8n / scripts / agentes / bots)

Demos de automatización e IA para negocios y ventas. No siempre se "despliegan" como web: se muestran con README + video + diagrama + código.

## Reglas:
1. Una carpeta por automatización: `01-cotizador-auto`, `02-bot-whatsapp-ventas`, etc.
2. Cada una debe tener `README.md` con: problema → solución → diagrama → cómo replicarlo + `workflow.json` (si es n8n) o código.

## Próximos:
- [ ] `01-cotizador-auto` — Form → PDF → WhatsApp/email + CRM
- [ ] `02-bot-whatsapp-ventas` — Bot RAG que responde y agenda
- [ ] `03-lead-scoring-ia` — Scoring automático de leads
